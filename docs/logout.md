# login
This file describes the `/app/logout-user` endpoint.

## Core details
* Endpoint: `/app/logout-user`.
* Request method: `POST`.
* Body parameters:
    * `token`: string.
* Response parameters:
    * `message`


Message relays whether the logout was successful or not. 
